// se le asigna automaticamente el indice a los valores que hemos agregado en el arreglo
let numeros = [20, 3, 45, 'hola', 67, 'esteban']

// accedemos a los valores del arreglo
// leer los valores
for(let i = 0; i < numeros.length; i++){
    console.log(numeros[i])
}
